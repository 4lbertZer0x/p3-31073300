<%- include('../partials/header') %>
        <main class="main-content user-dashboard">
            <div class="dashboard-header">
                <h2>Bienvenido, <%= user.username %></h2>
                <p>Tu actividad en CineCríticas</p>
            </div>

            <div class="dashboard-stats">
                <div class="stat-card">
                    <h3><%= reviews.length %></h3>
                    <p>Críticas Escritas</p>
                </div>
                <div class="stat-card">
                    <h3><%= reviews.reduce((acc, review) => acc + review.rating, 0) / reviews.length || 0 %></h3>
                    <p>Rating Promedio</p>
                </div>
            </div>

            <div class="reviews-section">
                <div class="section-header">
                    <h3>Mis Críticas Recientes</h3>
                    <a href="/user/reviews" class="btn btn-primary">Escribir Nueva Crítica</a>
                </div>

                <% if (reviews.length > 0) { %>
                    <div class="reviews-list">
                        <% reviews.forEach(review => { %>
                            <div class="review-item">
                                <div class="review-info">
                                    <h4><%= review.title %></h4>
                                    <span class="review-type <%= review.type %>">
                                        <%= review.type === 'movie' ? '🎬 Película' : '📺 Serie' %>
                                    </span>
                                </div>
                                <div class="review-rating">
                                    <% for (let i = 1; i <= 5; i++) { %>
                                        <span class="star <%= i <= review.rating ? 'filled' : '' %>">⭐</span>
                                    <% } %>
                                    <span class="rating-number">(<%= review.rating %>/5)</span>
                                </div>
                                <p class="review-content"><%= review.review %></p>
                                <div class="review-meta">
                                    <span class="review-date">Escrita el: <%= review.date %></span>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="empty-state">
                        <p>No has escrito ninguna crítica todavía.</p>
                        <a href="/user/reviews" class="btn btn-primary">Escribir Mi Primera Crítica</a>
                    </div>
                <% } %>
            </div>
        </main>
<%- include('../partials/footer') %>